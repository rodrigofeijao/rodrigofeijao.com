export default function pagesReducer( pages = ['homepage', 'clients', 'work', 'about', 'contacts'], action) {

    switch(action.type) {

        case "INCREMENT" : {
            break;
        }

        case "DECREMENT" : {
            break;
        }
    }

    return pages

}